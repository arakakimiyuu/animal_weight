      <!--非同期通信化のためlocal: trueを削除-->
      <%= form_with model: comment, url: post_comments_path(post_id: params[:post_id] || params[:id])  do |f| %>
        <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class:"form-control" %>
        <%= f.submit "送信する", class: "mt-3" %>
      <% end %>